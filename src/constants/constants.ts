export const TOAST_DURATION: number = 5 * 1000;
export const TOKEN_STORAGE = "auth";
export const TOKEN_HEADER = "authorization";
export const TOKEN_ID = "userID";

export enum Roles {
    USER = "USER",
    ADMIN = 'ADMIN'
}

export const ONE_MINUTE: number = 60 * 1000;